{{- if not .Values.auth.existingSecret }}
{{- fail "auth.existingSecret is required — create a Secret with database-uri, payload-secret, oidc-client-id, and oidc-client-secret keys and reference it in values" }}
{{- end }}
