image:
  repository: postgres
  tag: "18-alpine"
  pullPolicy: IfNotPresent

replicas: 1

auth:
  database: csbc-service-catalogue
  username: csbc-service-catalogue
  existingSecret: csbc-service-catalogue-postgres-credentials
  secretKeys:
    password: password

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 100m
    memory: 256Mi

storage:
  size: 1Gi
  storageClass: ""

service:
  port: 5432

config:
  maxConnections: "50"
  sharedBuffers: "128MB"
